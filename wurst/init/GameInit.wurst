package GameInit
import Child
import Flag
import EntityManagement
import Presents

function startGame()
	new Child(vec3(-2327,3800,200.), Player(0))
	new Child(vec3(-2300,4000,200.), Player(6))
	/*redflag = new Flag(vec3(-3500,1200,0.), CreateUnit(Player(0),'hpea', -3500, 1200, 0.))
	redflag.redTeam = true*/
	startEntityLoop()

init
	getTimer().start(0.1, function startGame)
